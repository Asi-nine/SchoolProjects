#include "plotinteractiontest.h"

using namespace std;


/*void plotinteractiontest::test1(){
    MainWindow w = new MainWindow();
    QCustomPlot *bar;
    std::vector<std::pair <std::string, double>> list;
    list<<("apple",2)<<("banana",3)<<("chocolate",4);
    w.setupBarChart(bar,list);

    bool result = false;

    if(bar->interactions()){
        result = true;
    }
    QCOMPARE(result,true);
}*/
